venv/
__pycache__/
*.pyc
.streamlit/
.env
*.xlsx

# SYNA ya no persiste en un archivo SQLite local (ver SYNA_TRACKING_README.md,
# sección "Persistencia y Backup": ahora usa un Postgres externo vía
# SYNA_DATABASE_URL). Estas entradas quedan por si se usa un .db local
# para testing o para una migración puntual - nunca debe versionarse una
# base de datos real.
syna_tracking.db
*.db-journal
*.db-wal
*.db-shm
